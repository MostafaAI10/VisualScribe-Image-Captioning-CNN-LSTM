# VisualScribe-Image-Captioning-CNN-LSTM
A PyTorch image captioning system using a ResNet-50 CNN encoder and LSTM decoder - trained with teacher forcing, evaluated with BLEU scores, and compatible with Flickr8k, Flickr30k, and MS-COCO.
